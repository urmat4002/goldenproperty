export interface Options {
    city?: string,
    dubai?: string;
    istanbul: string;
    antalya?: string;
    all?: string;
    chboxDubay?: boolean;
    chboxIstanbul?: boolean;
    chboxAntalya?: boolean;
    ChboxAll?: boolean;
}
export interface Checkbox {
    chboxDubay?: boolean;
    chboxIstanbul?: boolean;
    chboxAntalya?: boolean;
    ChboxAll?: boolean;
}